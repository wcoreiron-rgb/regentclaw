[lint]
# Catch real errors: syntax errors, undefined names, whitespace.
# F841 (unused local variable) is ignored — pre-existing pattern throughout codebase.
select = ["E9", "F8", "W"]
ignore = ["F841"]
